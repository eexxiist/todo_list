const operations = require('../utils/Math');
const stringOperations = require('../utils/String')
const assert = require('assert');

// it('should sum two numbers', () => {
//     let expected = 15,
//         result = operations.sum(7, 8);

//     if(expected !== result){
//         throw new Error(`Expected: ${expected}, but got: ${result}`);
//     }
// })


// const data = [
//     ['string', 'Ivan', 'Saint-Petersburg', 'Saint-Petersburg'], // Saint-Petersburg
//     ['Moscow', 'mother', 'value', 'Moscow'], // mother
//     ['not', 'a', 'number', 'number'], // number
//     ['obsession', 'current', 'peach', 'happy', 'obsession'], // obsession
//     ['father', 'cat', 'dog', 'father'], // father
// ] 

// for(let array of data){
//     it('Should find max length word', () => {
//         let expected = array[array.length - 1],
//             result = stringOperations.maxLengthString(array.slice(0, array.length - 1));

//         assert.equal(result, expected)
//     })
// }





// it('should div two numbers', () => {
//     let expected = 10,
//         result = operations.div(11, 1);

//     if(expected !== result){
//         throw new Error(`Expected: ${expected}, but got: ${result}`);
//     }
// })




// Написать проверку почты из массива в 20 строк
